<aura:component >
    <aura:attribute name="number" type="String" default="No Phone Number"/>
    <aura:handler event="c:PhoneNumberEvent" action="{!c.receive}"/>
    <ui:outputText aura:id="phone" value="{!v.number}"/>
</aura:component>